# Lunova Client (danışan) Frontend - Environment Variables
# Kopyala: cp .env.example .env
# Docker Compose backend'i host'ta 8000 portuna maplediği için bu değer
# Docker'lı ve Docker'sız çalışmada aynıdır.
VITE_API_BASE_URL=http://localhost:8000
